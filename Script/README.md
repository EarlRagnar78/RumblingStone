# Script/ — convertitori di contenuto

> **Disambiguazione**: questa cartella (S maiuscola) contiene i
> **convertitori di contenuto**; l'automazione DM (prep/post sessione,
> recap, mappe, CLI `dm.py`) vive in **`scripts/`** (minuscolo) — vedi
> `scripts/README-automation.md` e ADR-0002 in `plans/adr/`.

| Tool | Cosa fa |
|---|---|
| `pdf-to-md-engine/` (+ `tools/md2pdf.sh`) | PDF → markdown (e ritorno md → PDF) |
| `Html_to_markdown/` | HTML → markdown |
| `Image-to-webp/` | immagini → webp |
